<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-gray-100 min-h-screen p-8">

    <a href="/ "
    <button type="submit"
    class="w-full bg-blue-500 text-white font-bold py-2 px-4 rounded-lg hover:bg-blue-600 transition duration-200">
   go  to home
  </button></a>

    <div class="max-w-4xl mx-auto">
        <h1 class="text-4xl font-bold text-gray-800 mb-8 text-center"> ..All Users</h1>

        
                 
            </div>

             <% if(users.length > 0) { %> 
                
            <% users.forEach(function(user){ %>
                <div class="bg-white rounded-lg shadow-md p-6 hover:shadow-lg transition duration-300">
                    <div>
                        <img  src="<%=user.image%>" alt="ye hai img">
                        <h3 class="text-xl font-semibold text-gray-800 mb-2"> <%=user.name%></h3>
                        <h5 class="text-gray-600 mb-4"><%=user.email%></h5>
         
                        <div class="flex gap-4">
                            <a href="/edit/<%=user._id%>" class="px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition duration-300">Edit user</a>
                            <a href="/delete/<%=user._id%>" class="px-4 py-2 bg-red-500 text-white rounded hover:bg-red-600 transition duration-300">Delete user</a>
                        </div>
                    </div>
                </div>
            <% }) %>
            <% }else { %>
                <h1 class="text-2xl  text-center"> no useres yet </h1>
                <% } %>
        </div>
    </div>
</body>
</html>